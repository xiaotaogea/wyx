<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zjwm.wyx.login.dao.UserDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.zjwm.wyx.login.entity.UserEntity" id="userMap">
        <result property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="mobile" column="mobile"/>
        <result property="password" column="password"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

	<select id="queryObject" resultType="com.zjwm.wyx.login.entity.UserEntity">
		select * from hbb_user where id = #{id}
	</select>


	<select id="queryByMobile" resultType="com.zjwm.wyx.login.entity.UserEntity">
		select * from hbb_user where mobile = #{mobile}
	</select>
	 





</mapper>